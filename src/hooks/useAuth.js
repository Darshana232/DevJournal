import { useAuth } from '../context/AuthContext';

/**
 * Convenience hook exposing auth state and actions.
 * Components should use this instead of useContext(AuthContext) directly.
 */
export { useAuth };
